---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog');
const tags = [...new Set(allPosts.map((post) => post.data.tags || []).flat())];
---

<Layout title="Tags | Sven Wasmer" description="Browse blog posts by tag.">
	<h1 style="font-size: 3rem; margin-bottom: 3rem;">Tags</h1>
	
	<div style="display: flex; gap: 1rem; flex-wrap: wrap;">
		{tags.length > 0 ? tags.map((tag) => (
			<a href={`/tags/${tag}`} style="background: rgba(255,255,255,0.05); border: 1px solid var(--card-border); padding: 0.25rem 0.75rem; border-radius: 99px; font-size: 0.875rem; color: var(--text-color); transition: all 0.2s ease; display: inline-block;">
				{tag}
			</a>
		)) : <p>No tags found.</p>}
	</div>
</Layout>

<style>
	a:hover {
		transform: translateY(-2px);
		box-shadow: 0 5px 15px var(--accent-glow);
		border-color: var(--accent);
	}
</style>
